PORT=5001
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=root
DB_NAME=propmanager
JWT_SECRET=your_jwt_secret_key_here_change_in_production

# Tenant invite email (optional). Leave blank to log invites to the console
# instead of sending real email.
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
SMTP_FROM=
APP_URL=http://localhost:5173/login
